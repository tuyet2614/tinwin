import {SafeAreaView, Text} from 'react-native';

const Delivered: React.FC = () => {
  return (
    <SafeAreaView>
      <Text>Delivered</Text>
    </SafeAreaView>
  );
};

export default Delivered;
