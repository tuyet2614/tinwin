import { SafeAreaView, Text } from 'react-native';

const Price: React.FC = () => {
    return (
        <SafeAreaView>
            <Text>Price</Text>
        </SafeAreaView>
    );
};

export default Price;
